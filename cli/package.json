{
  "name": "tokenix-cli",
  "version": "0.1.0",
  "description": "Tokenix AI Compute Price Index — terminal client",
  "type": "module",
  "bin": {
    "tokenix": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "conf": "^13.0.1",
    "ora": "^8.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}

node_modules/
package-lock.json

# Build / framework caches
.next/
.turbo/

# Price snapshots (large, ephemeral — acpi_history.csv in dashboard/data/ is the record)
token_prices_*.csv
prices_*.csv
acpi_history.csv

# …but the calculator's cleaned export IS a committed artifact — it ships with
# the deploy and is read by /api/calculator/prices. Negated after the glob above.
!dashboard/data/prices_latest.csv

# Python
__pycache__/
*.pyc

# Misc
.DS_Store
.env*
